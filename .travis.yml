sudo: required
language: java
jdk: openjdk14

services:
  - mysql

script:
  - cd medihub-api && mvn install -DskipTests=false -Dmaven.javadoc.skip=true -B -V

branches:
  only: 
    - master

deploy:
  provider: heroku
  api-key:
    secure: $HEROKU_API_KEY
  app: medihub-app